# Base de datos — usa una URL completa (Railway, Render, Supabase, etc.)
DATABASE_URL=postgresql://user:password@localhost:5432/smlab

# JWT
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio

# Servidor
PORT=4000
NODE_ENV=development

# CORS — URL del frontend (sin barra final)
FRONTEND_URL=http://localhost:5173

# Ruta a las imágenes para los PDFs (copiar header_spectrummedia.png y firma.png aquí)
ASSETS_PATH=./src/assets
